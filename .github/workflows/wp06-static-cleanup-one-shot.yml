name: WP06 One-Shot Static Cleanup

on:
  push:
    branches:
      - agent/wp-06-loreitems-integration

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact branch head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: agent/wp-06-loreitems-integration
          fetch-depth: 0

      - name: Apply assertion-guarded static cleanup
        run: python3 tools/wp06_static_cleanup_patch.py

      - name: Remove one-shot helper and workflow
        run: |
          rm tools/wp06_static_cleanup_patch.py
          rm .github/workflows/wp06-static-cleanup-one-shot.yml

      - name: Commit exact cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/org/enthusia/tags/rewards/RewardService.java \
            src/main/java/org/enthusia/tags/rewards/RewardStorage.java \
            tools/wp06_static_cleanup_patch.py .github/workflows/wp06-static-cleanup-one-shot.yml
          git diff --cached --exit-code && { echo "Expected cleanup produced no changes" >&2; exit 1; } || true
          git commit -m "WP-06: resolve final static recovery findings"
          git push origin HEAD:agent/wp-06-loreitems-integration
