<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.enthusia</groupId>
    <artifactId>EnthusiaTags</artifactId>
    <version>2.2.2-pilot.2</version>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <!-- Match verified local builds: isolate javac from Maven's in-process compiler. -->
        <maven.compiler.fork>true</maven.compiler.fork>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <rosechat.contract.source>${project.basedir}/.ci-deps/rosechat/PresenceMessageEvent.java</rosechat.contract.source>
        <loreitems.release.version>1.0.0</loreitems.release.version>
        <loreitems.release.url>https://github.com/wsg138/EnthusiaLoreItems/releases/download/v1.0.0/EnthusiaLoreItems.jar</loreitems.release.url>
        <loreitems.release.sha256>7c862b0ae545d710a33267ad6e19a4ae26d97323e97f40707c1475c9f9ba7063</loreitems.release.sha256>
        <loreitems.release.jar>${project.basedir}/.wp06-deps/EnthusiaLoreItems.jar</loreitems.release.jar>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/releases/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.github.badgersmc</groupId>
            <artifactId>EnthusiaAdvancements-pilot</artifactId>
            <version>1.0.0-pilot.5</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>26.2.build.124-stable</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>2.11.6</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.github.alexdev03</groupId>
            <artifactId>unlimitednametags-api-paper</artifactId>
            <version>2.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.enthusia</groupId>
            <artifactId>EnthusiaLoreItems-released</artifactId>
            <version>${loreitems.release.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.53.2.0</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
        <dependency><groupId>org.mockito</groupId><artifactId>mockito-core</artifactId><version>5.20.0</version><scope>test</scope></dependency>
    </dependencies>

    <build>
        <finalName>EnthusiaTags</finalName>
        <plugins>
            <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-compiler-plugin</artifactId><version>3.13.0</version></plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>verify-pinned-loreitems-release</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <checksum file="${loreitems.release.jar}" algorithm="SHA-256"
                                          property="loreitems.actual.sha256"/>
                                <condition property="loreitems.release.sha256.matches">
                                    <equals arg1="${loreitems.actual.sha256}"
                                            arg2="${loreitems.release.sha256}"/>
                                </condition>
                                <fail unless="loreitems.release.sha256.matches"
                                      message="Pinned EnthusiaLoreItems release JAR checksum mismatch"/>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>verify-shaded-sqlite-driver</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <condition property="sqlite.shaded.driver.present">
                                    <resourceexists>
                                        <zipentry
                                            zipfile="${project.build.directory}/EnthusiaTags.jar"
                                            name="org/sqlite/JDBC.class"/>
                                    </resourceexists>
                                </condition>
                                <fail unless="sqlite.shaded.driver.present"
                                      message="Shaded EnthusiaTags JAR is missing the original-namespace SQLite JDBC driver"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <systemPropertyVariables>
                        <rosechat.contract.source>${rosechat.contract.source}</rosechat.contract.source>
                        <loreitems.release.jar>${loreitems.release.jar}</loreitems.release.jar>
                        <loreitems.release.sha256>${loreitems.release.sha256}</loreitems.release.sha256>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- Retain JNI/callback classes that cannot be discovered by bytecode-only minimization. -->
                            <minimizeJar>false</minimizeJar>
                            <!-- SQLite native JNI bindings require their original org.sqlite package names. -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
